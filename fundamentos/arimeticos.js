/**
 * os o operadores aritimeticos são binarios 
 * por existe dois operandos
 */
const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a 
const modulo = d % 2 // resto divição de a por 2
// se for igual = 0 é impar e se for = 1 par

console.log(soma, subtracao, multiplicacao, divisao, modulo)
