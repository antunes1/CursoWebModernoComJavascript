const a = 3
let b = 6

b += a // b = b + a / atribuição aditiva
console.log(b)

b -= a // b = b - a
console.log(b)

b *= 2 // b = b * 2
console.log(b)

b /= 2 // b = b / 2
console.log(b)

b %= 2 // atribuicao modular b % 2 - se resto a divição por par resto 0 , se a divição for com numero impar o resto sera 1 
console.log(b)

